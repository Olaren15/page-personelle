name: Déploiement

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/build.yml

  deploiement:
    needs: build
    uses: ./.github/workflows/deploiement-firebase.yml
    with:
      firebaseChannelId: live
